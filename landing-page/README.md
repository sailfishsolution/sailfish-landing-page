# Sailfish Solution — Landing Page

Local dev:

```powershell
npm install
cd landing-page
npm run dev
```

Build & start:

```powershell
cd landing-page
npm run build
npm run start
```

Notes:
- App Router (Next.js 14+) structure expected under `app/`.
- `landing-page/config/tools.js` drives the Tools dropdown and `/tools/[slug]` routes.
